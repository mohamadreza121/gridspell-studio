import Link from "next/link"; import { PageIntro } from "@/components/ui/PageIntro"; import { Container } from "@/components/ui/Container"; import { featuredProjects } from "@/config/work";
export default function Page(){return <><PageIntro eyebrow="Selected work" title="Designed with a reason." description="Each case study explains the business problem, the strategic choices, the digital system, and the outcome."/><section className="py-20 lg:py-28"><Container className="grid gap-6">{featuredProjects.map((p,i)=><Link key={p.slug} href={`/work/${p.slug}`} className="rounded-[2rem] border border-white/[.09] bg-white/[.025] p-8 hover:bg-white/[.045]"><p className="font-mono text-xs text-[#8be9ff]">{String(i+1).padStart(2,"0")} · {p.category}</p><h2 className="mt-7 font-display text-4xl font-semibold tracking-[-.055em]">{p.title}</h2><p className="mt-5 max-w-3xl leading-8 text-white/42">{p.description}</p></Link>)}</Container></section></>}
